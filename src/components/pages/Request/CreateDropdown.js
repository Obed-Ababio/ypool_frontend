import React from 'react';
import Destination from './Destination';

const CreateDropdown = (destination) => {
    return (
        <Destination place={destination} />
    )
}

export default CreateDropdown;
